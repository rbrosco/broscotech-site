/**
 * LEGACY FILE (do not use).
 *
 * This project uses the Next.js App Router.
 * The active register endpoint is implemented at: src/app/api/register/route.ts
 *
 * Keeping this file as an empty module prevents accidental usage and avoids
 * shipping hardcoded credentials.
 */
export {};
